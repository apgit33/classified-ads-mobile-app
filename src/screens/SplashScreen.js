import React from "react";
import { Button, Text, TextInput, View } from 'react-native';

export const SplashScreen = () => {
    return (
        <View>
            <Text>Loading...</Text>
        </View>

    );
}